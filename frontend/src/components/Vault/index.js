const Vault = () => {
  return (
    <div style={{ textAlign: "center" }}>
      <h1>
        this is the feelings vault component where you can recover deleted
        entries with your vault key
      </h1>
    </div>
  );
};

export default Vault;
